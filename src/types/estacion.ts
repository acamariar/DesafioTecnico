export type estacion = {
    codigo: number;
    nombre: string;
}

export type RawEstacion = {
    Estacion: string;
}
export type rankingEstacionFacturacion = {
    nombre: string;
    facturacion: number;
}